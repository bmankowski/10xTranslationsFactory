<!-- ---
import Layout from "../../layouts/Layout.astro";
import { AuthFormLayout } from "../../components/auth/AuthFormLayout";
import { Button } from "../../components/ui/button";
import { CheckCircle2 } from "lucide-react";

// Set to SSR mode to ensure it's always rendered on the server
export const prerender = false;

// Get success type from query string
const type = Astro.url.searchParams.get('type') || 'generic';

let title = "Success";
let message = "Operation completed successfully.";
let btnText = "Continue";
let btnHref = "/";

// Configure message based on type
switch (type) {
  case 'registration':
    title = "Registration Successful";
    message = "Your account has been created successfully. Please check your email to verify your account.";
    btnText = "Continue to Login";
    btnHref = "/auth/login";
    break;
  case 'verification':
    title = "Email Verified";
    message = "Your email has been verified successfully. You can now sign in to your account.";
    btnText = "Sign In";
    btnHref = "/auth/login";
    break;
  case 'password-reset':
    title = "Password Reset";
    message = "Your password has been reset successfully. You can now sign in with your new password.";
    btnText = "Sign In";
    btnHref = "/auth/login";
    break;
}
---

<Layout title={title}>
  <main class="flex flex-col items-center justify-center min-h-screen py-12 bg-background">
    <AuthFormLayout title={title} client:load>
      <div class="flex flex-col items-center space-y-4 py-4">
        <div class="text-center space-y-4">
          <div class="flex justify-center">
            <CheckCircle2 className="size-16 text-green-500" />
          </div>
          <p class="text-muted-foreground">{message}</p>
          <div>
            <a href={btnHref}>
              <Button>{btnText}</Button>
            </a>
          </div>
        </div>
      </div>
    </AuthFormLayout>
  </main>
</Layout>  -->